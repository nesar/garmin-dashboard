# secrets — NEVER commit
.env
*.env
.garminconnect/

# raw data & local store (regenerated by ingest)
data/fit/*.fit
data/fit/*.FIT
store/*.parquet
live/hr_log.csv

# python
__pycache__/
*.pyc
.venv/
venv/

# keep docs/ (that's the published site) but ignore local screenshots
*.png
!docs/**
